# Hooks

This directory stores hook scripts intended to support Claude-assisted development workflows.

At this stage these hooks are scaffolding only.

They are meant to evolve into:

- pre-commit validation
- pre-PR validation
- dangerous command blocking
- documentation gate checks

Do not assume they are automatically active yet.

Activation and runtime integration must be documented separately.